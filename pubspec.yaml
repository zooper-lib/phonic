name: phonic
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  convert: ^3.1.2
  zooper_flutter_encoding_utf16: ^0.0.1
  equatable: ^2.0.7
  collection: ^1.18.0
  quiver: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
