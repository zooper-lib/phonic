name: phonic
description: "A pure Dart package for audio file metadata manipulation."
version: 0.0.1
homepage:

environment:
  sdk: ^3.9.0

dependencies:
  convert: ^3.1.2
  zooper_flutter_encoding_utf16: ^0.0.1
  equatable: ^2.0.7
  collection: ^1.18.0
  quiver: ^3.2.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.26.2
